<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.sarah.ateliergl">

    <application
        android:allowBackup="true"
        android:icon="@drawable/logo_house_services2"
        android:label="@string/app_name"
        android:roundIcon="@drawable/logo_house_services2"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".InscriptionActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".ServiceActivity" />
        <activity android:name=".PasswordOublierActivity"/>
        <activity android:name=".MainActivity"/>
    </application>

</manifest>