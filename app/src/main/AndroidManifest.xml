<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.sarah.ateliergl">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_home_services"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_home_services_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".ProfilsActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".ServiceActivity" />
        <activity android:name=".PasswordOublierActivity"/>
        <activity android:name=".InscriptionActivity"/>
        <activity android:name=".LoginActivity"/>
        <activity android:name=".activity_profil"/>
    </application>

</manifest>